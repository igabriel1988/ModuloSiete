package dgtic.unam.modulosieta

import java.security.cert.CertPath

data class Modelo(
                        var nameFile    : String,
                        var nameImage   : Int,
                        var type        : Int
                 ) {
}